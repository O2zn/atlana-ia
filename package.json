{
  "name": "atlana-agent",
  "version": "1.0.0",
  "description": "Atlana — Lean AI Headless Security Agent for IaC (Terraform, Docker)",
  "main": "src/scripts/scan-cli.ts",
  "scripts": {
    "scan": "tsx src/scripts/scan-cli.ts",
    "test": "jest"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "roots": [
      "<rootDir>/src"
    ],
    "moduleNameMapper": {
      "^@/(.*)$": "<rootDir>/src/$1"
    }
  },
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/jest": "^30.0.0",
    "@types/node": "^20",
    "jest": "^30.3.0",
    "ts-jest": "^29.4.9",
    "tsx": "^4.21.0",
    "typescript": "^5"
  }
}
